import { StyleSheet } from 'react-native'

export default styles = StyleSheet.create({
    container:{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center'
    },
    secondContainer: {
        flex: 1,
        flexDirection: 'row',
        margin: 20
    },
    flatContainer:{
        flex: 15
    }
})